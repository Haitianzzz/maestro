# Tiny Flask app

Fixture repo used by Maestro's planner tests. Not a real Flask project.

## Structure

- `src/app.py` — the main application entry.
- `src/models/user.py` — a tiny SQLAlchemy-style User model.
- `src/db.py` — database stub.
